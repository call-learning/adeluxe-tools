# adeluxe-tools

This draft contains three parts:

- `installer/`: a Composer plugin (`adeluxe-tools/clapi-installer`).
- `package/`: the reusable CLAPI source package (`adeluxe-tools/moodle-clapi`).
- `example-plugin/`: a Moodle plugin root showing selective module installation.

## Selective installation

The consuming Moodle plugin decides what it wants:

```json
"extra": {
  "clapi": {
    "component": "mod_example",
    "target": ".",
    "modules": ["repository", "modal"]
  }
}
```

If it only wants repository helpers:

```json
"modules": ["repository"]
```

The CLAPI package is stored under `.clapi/packages/` rather than the normal `vendor/` path, while selected source is materialised into Moodle-native locations:

- PHP -> `classes/local/clapi/...`
- AMD source -> `amd/src/local/clapi/...`
- Mustache -> `templates/local/clapi/...`
- strings -> merged into `lang/en/<component>.php` between generated markers

`__COMPONENT__` is replaced with the target Moodle component, so the same CLAPI source can become either:

- `mod_example\\local\\clapi\\repository\\base_repository`
- `local_whatever\\local\\clapi\\repository\\base_repository`

and AMD/template names likewise become component-local.

## Generated file ownership

`.clapi/generated.json` records files generated by CLAPI. When a module is removed from the configured module list, files previously generated for that module are deleted without touching unrelated plugin files.

## Suggested development workflow

Treat `adeluxe-tools/moodle-clapi` as a development/build dependency and commit the materialised Moodle source to the plugin repository. The distributed Moodle plugin then has no runtime dependency on Composer or another Moodle plugin.

## Important draft limitations

This is deliberately a first implementation rather than a production release. Before publishing it I would add:

1. File hashes so CLAPI refuses to overwrite a generated file that a developer modified locally.
2. Per-language language resources rather than English only.
3. Module dependencies, e.g. `datatable` can declare `requires: [repository]`.
4. Conflict detection when two CLAPI packages generate the same target path/string key.
5. A `composer clapi:sync` command in addition to automatic sync after autoload generation.
6. Tests using Composer's test utilities and temporary Moodle-plugin fixtures.
7. Optional groups/presets such as `forms`, `ui`, or `all`, without making `all` the default.
